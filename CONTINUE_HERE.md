# CONTINUE_HERE — Remaining Tasks

## Current State (2026-02-09)

**kayoutf package**: v0.1.0, 0 errors / 0 warnings / 0 notes, 167 tests passing.
**Gallery**: 749 images (616 classified + 133 unclassified), tagging system, source/tag filters.
**Data**: 1044 cards across 8 sets, 112 characters, 73 rarities.

---

## Priority 1: Feedback Persistence Investigation

The correct/incorrect feedback buttons may not be visually persisting across app restarts. The data layer is verified working (27 rows in `classification_feedback.csv`, keys match, round-trip tested). Needs manual investigation:

- [ ] Launch app, mark a card correct/incorrect, stop app, relaunch — do overlays appear?
- [ ] If not, check whether `load_feedback_data()` returns all rows on startup
- [ ] Check if the key format `filename|directory` at render time matches `filename|current_directory` at load time for all images (they should be identical for classified images)

## Priority 2: Image Classification Improvements

### Move misclassified images
The classification CSV has a `needs_move` / `move_to` column indicating images in the wrong set directory. No automated mover exists yet.

- [ ] Create `scripts/move_misclassified.R` — reads `classification_results.csv`, moves files where `needs_move == TRUE` to the `move_to` directory
- [ ] Re-run classification after moves to update CSV

### Reclassify with improved model
The current `classification_results.csv` was generated by a single LLM pass. Many images have low confidence or are non-card images (box photos, promo material).

- [ ] Develop a second-pass classification script that uses feedback data to improve accuracy
- [ ] Use human feedback (correct/incorrect) as training signal for re-classification

## Priority 3: Card Data Completeness

### TF01 — Fill remaining gaps
- [ ] Identify UR-008 character (7/8 known from 1688 promo images)
- [ ] Confirm SHR full checklist (only SHR-001=Optimus Prime confirmed)
- [ ] Fill in BP, LR, AR character names

### TF02 — Needs major research
- [ ] Only 4 individual cards confirmed; structure assumed same as TF01
- [ ] Search eBay, 1688, Xianyu for TF02 card images with visible numbers/characters

### TF03 — Fill character names
- [ ] SSR fully confirmed (16 combiner team cards) but other rarities need character data
- [ ] R/SR/HR/AR counts confirmed, need character assignments

### TFKB01 — Likely incomplete
- [ ] Only 3 rarities (AR/HR/SR) from physical card denominators
- [ ] May have additional rarities not yet discovered
- [ ] Research whether TFKB01 has its own product or is only a TF02 box insert

### TFH01 — Fill character names
- [ ] All counts from eBay denominators, but most character names missing
- [ ] SL (Scene Landscape) cards need identification

### TFO01 — Fill remaining character names
- [ ] TP (The Primes) subset: confirm all 13 Primes character assignments
- [ ] UR-S (UR Special) variant details needed

### TF40Y — Verify Skywarp/Thundercracker swap
- [ ] Document the manufacturing error (names switched on all cards)
- [ ] Decide whether to store as-printed or corrected names in database

## Priority 4: Gallery Enhancements

### Tag-based workflows
- [ ] Add bulk tagging (select multiple images, apply tag to all)
- [ ] Add tag-based export (export all images with a given tag)
- [ ] Pre-populate suggested tags: set codes, rarity codes, character names

### Image quality indicators
- [ ] Flag ~80 thumbnail images (220px) as low-quality in gallery
- [ ] Show image dimensions in modal detail view
- [ ] Allow filtering by image quality (thumbnail vs full-res)

### Gallery search
- [ ] Add full-text search across filename, character name, notes
- [ ] Add keyboard navigation (arrow keys for next/prev image in modal)

## Priority 5: Package Distribution

### pkgdown site
- [ ] Build pkgdown documentation site
- [ ] Deploy to GitHub Pages
- [ ] Add vignettes: "Getting Started", "Card Data Schema", "Gallery Usage"

### CRAN preparation
- [ ] Review CRAN submission checklist
- [ ] Ensure all examples run in < 5 seconds
- [ ] Add `\donttest{}` around slow examples
- [ ] Write `cran-comments.md`

## Priority 6: Data Pipeline

### Automated web scraping
- [ ] Refine eBay scraper (`scripts/scrape_card_images.R`) for ongoing monitoring
- [ ] Add 1688.com scraper for Chinese market images
- [ ] Add Xianyu (闲鱼) scraper for secondary market data

### Price tracking
- [ ] Design price_history table schema
- [ ] Track eBay sold listings for market value data
- [ ] Add `kt_prices()` accessor function

---

## File Reference

| File | Purpose |
|------|---------|
| `kayoutf/R/utils.R` | Gallery utilities: scan_image_directories, build_gallery_data, load/save/remove tags |
| `kayoutf/inst/app/app.R` | Shiny gallery with filters, feedback, tagging |
| `scripts/convert_avif.sh` | AVIF to JPEG conversion for manual_downloads |
| `scripts/classification_results.csv` | 616 classified images from eBay scraper |
| `scripts/classification_feedback.csv` | Human correct/incorrect feedback (append-only) |
| `scripts/image_tags.csv` | Free-form image tags (append-only) |
